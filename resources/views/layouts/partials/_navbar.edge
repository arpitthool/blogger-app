<nav class="navbar is-link">
    <div class="container">
        <div class="navbar-brand">
            {{--  <a href="/" class="navbar-item">Blogger</a>  --}}
            <a href="{{ route('home') }}" class="navbar-item">Blogger</a>
            <a href="{{ route('about') }}" class="navbar-item">About</a>
            <a href="{{ route('contact') }}" class="navbar-item">Contact</a>
            <a href="{{ route('greeting') }}" class="navbar-item">Greeting</a>
        </div>
    </div>
</nav>